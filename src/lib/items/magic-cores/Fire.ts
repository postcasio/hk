import MagicCore from '../MagicCore';
//import Element from '../../magic/Element';
import uitheme from '../../uitheme';

export default class Fire extends MagicCore {
  name = 'Fire';

  coreColor = uitheme.colors.cores.red;
}
